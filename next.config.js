/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', 'plus.unsplash.com', 'images.unsplash.com', 'cdn.buymeacoffee.com']
    },
    output: 'export'
}

module.exports = nextConfig
